# 작성 규칙

이 사이트의 독자는 AI를 한 번도 써 보지 않은 사람이다. 모든 페이지는 이 독자를 기준으로 쓴다.

## 문체
- 존댓말 해요체와 합니다체를 섞지 말고 합니다체로 통일한다.
- 전문 용어는 처음 나올 때 쉬운 말로 풀어 쓴다. 풀 수 없으면 `appendix/glossary`에 추가하고 링크한다.
- 한 문장에 한 가지 내용만 쓴다. 문단은 3문장 이내로 끊는다.
- 영어는 서비스 이름과 버튼 이름처럼 화면에 그대로 보이는 것만 쓴다.

## 내용
- 무료 기능을 기준으로 설명한다. 유료 기능은 "유료" 표시를 붙이고 무료 대안을 함께 쓴다.
- 서비스 화면, 무료 한도, 요금은 자주 바뀐다. 쓸 때 기준 날짜를 `<Note>`로 남긴다.
- 페이지마다 독자가 직접 따라 해 볼 수 있는 예시 질문을 하나 이상 넣는다.
- 특정 유료 강의나 판매자를 실명으로 비난하지 않는다. 수법과 판별 기준만 설명한다.

## 페이지 구조
1. frontmatter: `title`, `description`, `icon`
2. 한두 문장 도입
3. 본문 (`<Steps>`, `<Tip>`, `<Warning>` 컴포넌트 적극 사용)
4. "직접 해 보기" 예시
5. "다음으로" 카드

## 새 페이지 추가
파일을 만든 뒤 `docs.json`의 해당 그룹 `pages`에 경로를 추가한다. 작업 후 `mint broken-links`로 확인한다.
